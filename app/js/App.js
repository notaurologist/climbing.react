/** @flow */

var React = require('react');
var RouteForm = require('./RouteForm');

var App = React.createClass({
  render(): any {
    return (
      <RouteForm/>
    );
  }
});

module.exports = App;
